<p id="notice"><%= notice %></p>

<h1>Explores</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @explores.each do |explore| %>
      <tr>
        <td><%= explore.title %></td>
        <td><%= explore.description %></td>
        <td><%= explore.price %></td>
        <td><%= link_to 'Show', explore %></td>
        <td><%= link_to 'Edit', edit_explore_path(explore) %></td>
        <td><%= link_to 'Destroy', explore, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Explore', new_explore_path %>
