<p id="notice"><%= notice %></p>

<h1>Explores</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @explores.each do |explore| %>
      <tr>
        <td><%= explore.title %></td>
        <td><%= explore.description %></td>
        <td><%= explore.price %></td>
        <td><%= link_to 'Show', explore %></td>
        <%# <% if @explore.user.email == current_user.email %>
           <td><%= link_to 'Edit', edit_explore_path(explore) %></td>
        <td><%= link_to 'Destroy', explore, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <%# <% end %> 
       
  
      </tr>
    <% end %>
  </tbody>
</table>

<br>

